To generate functions use `python3 GPT-CLI-main/automatedUseAPI.py`

To run the server use `node routes/todo_endpoints.js`.

To execute unit tests use `npx mocha tests/alltodoTests.js --reporter mochawesome`.